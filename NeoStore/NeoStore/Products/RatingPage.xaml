<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:NeoStore.CustomView;assembly=NeoStore"
             x:Class="NeoStore.Products.RatingPage" Padding="20,20,20,20" BackgroundColor="Transparent">

  <StackLayout>
    <local:GothamLabelText Text="{Binding ProductName}"
                           HorizontalOptions="CenterAndExpand"
                           FontSize="21"
                           TextColor="Black"
                           Font="Bold" />
    
    <Image x:Name="MainImage"
           Source="{Binding ProductImages}"
           VerticalOptions="CenterAndExpand"
           HorizontalOptions="CenterAndExpand"/>
    
    <StackLayout x:Name="DetailsRating"
                 Orientation="Horizontal"
                 VerticalOptions="CenterAndExpand"
                 HorizontalOptions="CenterAndExpand"/>
    
    <local:ColorButton Text="BUY NOW"
                       Font="Medium"
                       FontSize="22"
                       VerticalOptions="CenterAndExpand"
                       HorizontalOptions="FillAndExpand"
                       x:Name="buynow"
                       TextColor="White"/>

  </StackLayout>
</ContentPage>